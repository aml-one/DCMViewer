<UserControl x:Class="DCMViewer.Controls.DcmViewerCanvasComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:h="http://helix-toolkit.org/wpf"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000">
    <Grid x:Name="WatermarkCanvas">
        <Grid.Background>
            <RadialGradientBrush GradientOrigin="0.5,0.5" Center="0.5,0.5" RadiusX="0.8" RadiusY="0.8">
                <GradientStop Color="#FFFFFFFF" Offset="0.0" />
                <GradientStop Color="#FFF3F6F9" Offset="0.35" />
                <GradientStop Color="#FFD3D7DA" Offset="0.7" />
                <GradientStop Color="#FFB0B4B8" Offset="1.0" />
            </RadialGradientBrush>
        </Grid.Background>

        <Rectangle HorizontalAlignment="Left"
                   Width="14"
                   IsHitTestVisible="False"
                   Panel.ZIndex="0">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#FFF3F6F9" Offset="0" />
                    <GradientStop Color="#00F3F6F9" Offset="1" />
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Rectangle HorizontalAlignment="Right"
                   Width="14"
                   IsHitTestVisible="False"
                   Panel.ZIndex="0">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="1,0" EndPoint="0,0">
                    <GradientStop Color="#FFF3F6F9" Offset="0" />
                    <GradientStop Color="#00F3F6F9" Offset="1" />
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Image Source="pack://application:,,,/AmL.DCMViewer;component/Images/logo.png"
               MaxWidth="512"
               MaxHeight="512"
               Width="420"
               Height="420"
               Opacity="0.03"
               Stretch="Uniform"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               IsHitTestVisible="False"
               Panel.ZIndex="0" />

        <TextBlock Text="AmL"
                   FontSize="80"
                   FontWeight="SemiBold"
                   Foreground="#FFB8A35C"
                   Opacity="0.05"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   IsHitTestVisible="False"
                   Panel.ZIndex="0">
            <TextBlock.Effect>
                <DropShadowEffect ShadowDepth="0"
                                  BlurRadius="26"
                                  Opacity="0.16"
                                  Color="#FF000000" />
            </TextBlock.Effect>
        </TextBlock>

        <h:HelixViewport3D x:Name="Viewport"
                           ShowViewCube="True"
                           ShowCoordinateSystem="True"
                           IsRotationEnabled="True"
                           CameraRotationMode="Trackball"
                           RotateGesture="RightClick"
                           IsPanEnabled="True"
                           IsZoomEnabled="True"
                           RotateAroundMouseDownPoint="False"
                           ZoomExtentsWhenLoaded="True"
                           Background="Transparent">

            <h:HelixViewport3D.Camera>
                <PerspectiveCamera Position="{Binding CameraPosition}"
                                   LookDirection="{Binding CameraLookDirection}"
                                   UpDirection="{Binding CameraUpDirection}"
                                   FieldOfView="45" />
            </h:HelixViewport3D.Camera>

            <h:CuttingPlaneGroup x:Name="SectionCutGroup"
                                 IsEnabled="False"
                                 Content="{Binding ModelGroup}" />

            <ModelVisual3D x:Name="SectionPlaneVisual" />
            <h:LinesVisual3D x:Name="SectionPlaneOutlineVisual"
                             IsRendering="False"
                             Thickness="1.0"
                             Color="#FF15459F" />

            <h:LinesVisual3D x:Name="MeasurementLine"
                             Thickness="2.4"
                             Color="#FF1F6FEB"
                             IsRendering="False" />

            <h:BillboardTextVisual3D x:Name="MeasurementText"
                                     IsRendering="False"
                                     Background="#DDF5F9FF"
                                     BorderBrush="#FF8BA1B8"
                                     BorderThickness="1"
                                     Foreground="#FF1A2A3A"
                                     Padding="6,3"
                                     FontSize="13"
                                     FontWeight="SemiBold"
                                     Text="" />
        </h:HelixViewport3D>
    </Grid>
</UserControl>
